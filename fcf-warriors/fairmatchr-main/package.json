{
  "name": "iowa-state-fair",
  "version": "1.0.1",
  "private": true,
  "description": "A way for like-minded individuals to attend the state fair.",
  "scripts": {
    "db:create": "wrangler d1 create studybuddy-db",
    "db:init": "wrangler d1 execute studybuddy-db --remote --file schema.sql",
    "db:reset": "wrangler d1 execute studybuddy-db --remote --command \"DELETE FROM messages; DELETE FROM matches; DELETE FROM profiles;\"",
    "dev": "wrangler pages dev ./public",
    "deploy": "wrangler pages deploy",
    "set-key": "wrangler pages secret put ANTHROPIC_API_KEY"
  },
  "devDependencies": {
    "wrangler": "^4.124.0"
  }
}
